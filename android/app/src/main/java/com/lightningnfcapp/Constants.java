package com.lacrypta.cardinstaller;

/**
 * Shared constants. The TapLinX/NXP key material and aliases that used to live
 * here were removed along with the native TapLinX integration; only the logging
 * tag remains (used by the React modules and SecureStorage).
 */
public class Constants {
    static final String TAG = "cardinstaller";
}
